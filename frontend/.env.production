# Use Vercel API proxy — requests go to /api/* which proxies to EC2 backend
# The proxy uses BACKEND_API_URL from Vercel environment variables (set in dashboard)
# Leave VITE_API_URL unset to default to /api
# VITE_API_URL=
