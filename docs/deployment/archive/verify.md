# Verify Your AWS Deployment\n\n**This guide helps you confirm everything is working correctly.**\n\n---\n\n## ✅ Quick Verification (5 minutes)\n\n### 1. Lambda Function URL Works\n\n```bash\ncurl https://slysl7v476awcx4754dv5osg3e0jjzqf.lambda-url.us-east-1.on.aws/health\n```\n\n**Expected Response**:\n```json\n{\"status\":\"ok\",\"service\":\"JobApplicationTracker\",\"db_initialized\":false}\n```\n\n✅ **Status**: Working if you see `{\"status\":\"ok\"}`\n\n---\n\n### 2. Trigger a Job Sync\n\n```bash\ncurl -X POST https://slysl7v476awcx4754dv5osg3e0jjzqf.lambda-url.us-east-1.on.aws/api/jobs/sync\n```\n\n**Expected Response**:\n```json\n{\"status\":\"syncing\",\"sources_active\":5,\"next_sync_in_seconds\":3600}\n```\n\n✅ **Status**: Working if you see `\"status\":\"syncing\"`\n\n---\n\n### 3. Check CloudWatch Logs\n\n```bash\naws logs tail /aws/lambda/jobtracker-api --follow --region us-east-1\n```\n\n**Expected**: See log entries with timestamps, levels (INFO/ERROR), and messages\n\n✅ **Status**: Working if you see structured JSON logs\n\n---\n\n## 🔍 Detailed Verification (15 minutes)\n\n### Check Lambda Configuration\n\n```bash\naws lambda get-function --function-name jobtracker-api --region us-east-1 --query 'Configuration.[FunctionName,Runtime,MemorySize,Timeout,State]' --output text\n```\n\n**Expected Output**:\n```\njobtracker-api python3.11 512 300 Active\n```\n\n---\n\n### Check RDS Database\n\n```bash\naws rds describe-db-instances --db-instance-identifier jobtracker-db --region us-east-1 --query 'DBInstances[0].[DBInstanceIdentifier,Engine,DBInstanceStatus,Endpoint.Address]' --output text\n```\n\n**Expected Output**:\n```\njobtracker-db postgres available YOUR_RDS_ENDPOINT_HERE\n```\n\n---\n\n### Check S3 Bucket\n\n```bash\naws s3 ls s3://jobtracker-documents-245091941294/ --region us-east-1\n```\n\n**Expected**: Listing bucket contents (even if empty)\n\n---\n\n### Check EventBridge Rule\n\n```bash\naws events describe-rule --name jobtracker-daily-sync --region us-east-1 --query '[Name,ScheduleExpression,State]' --output text\n```\n\n**Expected Output**:\n```\njobtracker-daily-sync cron(0 13 * * ? *) ENABLED\n```\n\n---\n\n### Check EventBridge Targets\n\n```bash\naws events list-targets-by-rule --rule jobtracker-daily-sync --region us-east-1 --query 'Targets[0].[Id,Arn]' --output text\n```\n\n**Expected Output**:\n```\n1 arn:aws:lambda:us-east-1:245091941294:function:jobtracker-api\n```\n\n---\n\n## 🗄️ Database Verification\n\n### Connect to PostgreSQL\n\n```bash\npsql postgresql://jobadmin:YOUR_DB_PASSWORD_HERE@YOUR_RDS_ENDPOINT_HERE:5432/jobtracker\n```\n\n### List Tables\n\n```sql\n\\dt\n```\n\n**Expected**: Shows these 5 tables:\n- `profile`\n- `job`\n- `application`\n- `document`\n- `application_metric`\n\n### Count Records\n\n```sql\nSELECT \n  (SELECT COUNT(*) FROM jobs) as job_count,\n  (SELECT COUNT(*) FROM applications) as app_count,\n  (SELECT COUNT(*) FROM documents) as doc_count;\n```\n\n**Expected**: Shows row counts (initially 0 for new database)\n\n---\n\n## 📊 Test API Endpoints\n\n### Get All Jobs\n\n```bash\ncurl https://slysl7v476awcx4754dv5osg3e0jjzqf.lambda-url.us-east-1.on.aws/api/jobs?limit=5\n```\n\n**Expected Response**: JSON array of jobs (or empty if no jobs synced yet)\n\n---\n\n### Get Scheduler Status\n\n```bash\ncurl https://slysl7v476awcx4754dv5osg3e0jjzqf.lambda-url.us-east-1.on.aws/api/scheduler/jobs/status\n```\n\n**Expected Response**:\n```json\n{\n  \"status\":\"idle\",\n  \"last_sync_at\":\"2026-06-05T17:30:00\",\n  \"next_sync_at\":\"2026-06-06T13:00:00\",\n  \"interval_minutes\":1440\n}\n```\n\n---\n\n### Get Metrics\n\n```bash\ncurl https://slysl7v476awcx4754dv5osg3e0jjzqf.lambda-url.us-east-1.on.aws/api/metrics/dashboard?days=7\n```\n\n**Expected Response**: JSON object with metrics (initially 0s if no data)\n\n---\n\n## 🚨 Troubleshooting\n\n### Lambda Function URL Timeout\n\n**Problem**: `curl` command times out  \n**Solution**: \n1. Check internet connection\n2. Verify Function URL is correct\n3. Check CloudWatch logs for errors\n\n### Database Connection Failed\n\n**Problem**: `psql` connection refused  \n**Solution**:\n1. Check security group allows port 5432 from your IP\n2. Verify endpoint is correct\n3. Verify password is correct\n\n### EventBridge Not Triggering\n\n**Problem**: Lambda not invoked at 8 AM EST  \n**Solution**:\n1. Check rule is ENABLED: `aws events describe-rule --name jobtracker-daily-sync`\n2. Check targets are configured: `aws events list-targets-by-rule --rule jobtracker-daily-sync`\n3. Check Lambda permissions: `aws lambda get-policy --function-name jobtracker-api`\n\n### S3 Upload Fails\n\n**Problem**: Permission denied uploading to S3  \n**Solution**:\n1. Check Lambda IAM role has S3 permissions\n2. Check bucket name is correct\n3. Check bucket policy allows Lambda access\n\n---\n\n## 📝 Test Checklist\n\nRun through these and mark as complete:\n\n- [ ] Lambda health check responds\n- [ ] Job sync endpoint works\n- [ ] CloudWatch logs visible\n- [ ] Lambda configuration correct\n- [ ] RDS database available\n- [ ] S3 bucket accessible\n- [ ] EventBridge rule enabled\n- [ ] EventBridge targets configured\n- [ ] Database has all 5 tables\n- [ ] API endpoints respond\n- [ ] Metrics endpoint works\n- [ ] Scheduler status visible\n\n**All checked?** System is ready! ✅\n\n---\n\n## 🔄 Daily Verification (Quick Check)\n\nRun this daily at 8:15 AM EST to verify the scheduler ran:\n\n```bash\n# Check latest logs\naws logs tail /aws/lambda/jobtracker-api --since 1h --region us-east-1 | tail -5\n\n# Should show execution from 8:00 AM EST (13:00 UTC)\n```\n\n---\n\n## 📞 When to Reach Out\n\nIf you see:\n- ❌ **500 errors** → Check Lambda logs\n- ❌ **Connection refused** → Check RDS security group\n- ❌ **Permission denied** → Check IAM roles\n- ❌ **No logs** → Check CloudWatch log group exists\n\n---\n\n## ✨ You're All Set!\n\nIf all checks pass, your system is:\n- ✅ Deployed to AWS\n- ✅ Running correctly\n- ✅ Logging to CloudWatch\n- ✅ Scheduled to sync daily\n- ✅ Ready for Phase 4\n\n**Next Step**: Read `NEXT_STEPS_PHASE_4.md` to plan what builds next!\n"